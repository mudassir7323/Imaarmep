// theme.js
const theme = {
  colors: {
    primary: "#0d5a8a",        // Dark blue (for buttons, highlights)
    primaryLight: "#5aaee8",   // Light blue (accents, hover states)
    secondary: "#1f8dd6",      // Another blue variant
    accent: "#2176ff",         // Accent color (links, highlights)
    background: "#f9fafb",     // Light background
    textPrimary: "#111827",    // Main text color (dark)
    textSecondary: "#6b7280",  // Secondary text (gray)
    white: "#ffffff",          // White
    grayLight: "#e5e7eb",      // Light gray for borders, backgrounds
    grayDark: "#374151",       // Darker gray for text, UI elements
    error: "#dc2626",          // Red for errors
    success: "#16a34a",        // Green for success messages
  },
};

export default theme;